---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: config-unifi
  namespace: network
spec:
  storageClassName: rook-ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
