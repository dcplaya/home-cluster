# flux-system

## Bootstrap Flux

```bash
flux bootstrap github \
  --version=v0.3.0 \
  --owner=dcplaya \
  --repository=home-cluster \
  --path=cluster \
  --personal \
  --network-policy=false
```
