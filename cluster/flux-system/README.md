# flux-system

## Bootstrap Flux

```bash
flux bootstrap github \
  --version=v0.4.3 \
  --owner=dcplaya \
  --repository=home-cluster \
  --path=cluster \
  --personal \
  --network-policy=false
```
