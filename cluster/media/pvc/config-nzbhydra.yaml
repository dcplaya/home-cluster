---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: config-nzbhydra
  namespace: media
spec:
  storageClassName: rook-ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
