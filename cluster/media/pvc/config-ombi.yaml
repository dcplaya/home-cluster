---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: config-ombi
  namespace: media
spec:
  storageClassName: rook-ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
