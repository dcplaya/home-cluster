---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: gitea
  namespace: dev
spec:
  interval: 5m
  chart:
    spec:
      # renovate: registryUrl=https://dl.gitea.io/charts/
      chart: gitea
      version: 2.1.1
      sourceRef:
        kind: HelmRepository
        name: gitea-charts
        namespace: flux-system
      interval: 5m
  # dependsOn:
  # - name: sealed-secrets
  #   namespace: kube-system
  values:
    persistence:
      enabled: true
      existingClaim: config-gitea
    ingress:
      enabled: true
      annotations:
        kubernetes.io/ingress.class: "internal"
        nginx.ingress.kubernetes.io/proxy-body-size: 128m
      hosts:
      - "gitea.dev.elcarpenter.com"
      tls:
      - hosts:
        - "gitea.dev.elcarpenter.com"
    service:
      http:
        port: 3000
      # ssh:
      #   type: LoadBalancer
      #   port: 1337
      #   externalTrafficPolicy: Local
      #   externalIPs:
      #   - 192.168.69.101
    gitea:
      admin:
        username: "dcplaya"
        email: "dcplaya@gmail.com"
      config:
        APP_NAME: "Home Gitea"
        server:
          ROOT_URL: https://gitea.dev.elcarpenter.com
          DISABLE_SSH: true
          # SSH_PORT: 1337
          # SSH_LISTEN_PORT: 22
        respository:
          DEFAULT_PRIVATE: true
        admin:
          DISABLE_REGULAR_ORG_CREATION: true
        security:
          PASSWORD_COMPLEXITY: "lower,upper"
          MIN_PASSWORD_LENGTH: 12
        service:
          DISABLE_REGISTRATION: true
          REQUIRE_SIGNIN_VIEW: true
      database:
        builtIn:
          postgresql:
            enabled: true
      cache:
        builtIn:
          enabled: true
    memcached:
      image:
        repository: bitnami/memcached
        tag: 1.6.9
      service:
        port: 11211
    postgresql:
      image:
        repository: bitnami/postgresql
        tag: 11.10.0
      persistence:
        storageClass: "rook-ceph-block"
  valuesFrom:
  - kind: Secret
    name: "gitea-helm-values"
